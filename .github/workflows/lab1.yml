name: lab1
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: pip install nbconvert
      - uses: actions/checkout@v2
      - run: sudo apt-get install pandoc
      - run: python -m nbconvert --to pdf lab1.ipynb